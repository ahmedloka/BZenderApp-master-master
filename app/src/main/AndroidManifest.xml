<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="apps.pixel.bzender">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.example.maptest.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name="apps.pixel.bzender.Utills.MyApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="apps.pixel.bzender.activities.WebViewOfferActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home"></activity>
        <activity
            android:name="apps.pixel.bzender.activities.tenderRealEstateDetails.TenderRealEstateDetails"
            android:parentActivityName="apps.pixel.bzender.activities.TenderCarDetails" />
        <activity
            android:name="apps.pixel.bzender.activities.real_state.AddRealStateActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.before.BeforerActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.webPaymnet.WebPaymentActivity"
            android:parentActivityName="apps.pixel.bzender.activities.packages.PackagesActivity" />
        <activity
            android:name="apps.pixel.bzender.activities.PaymentActivity"
            android:parentActivityName="apps.pixel.bzender.activities.packages.PackagesActivity" />
        <activity
            android:name="apps.pixel.bzender.activities.packages.PackagesActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.myTender.MyTendersActivity"
            android:configChanges="orientation|keyboardHidden"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.TermsAndConditionActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Setting" />
        <activity
            android:name="apps.pixel.bzender.activities.imagesBook.ImagesActivity"
            android:parentActivityName="apps.pixel.bzender.activities.TenderDetails.TinderDetails" />
        <activity
            android:name="apps.pixel.bzender.activities.BookActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.TenderCarDetails"
            android:parentActivityName="apps.pixel.bzender.activities.TenderDetails.TinderDetails" />
        <activity
            android:name="apps.pixel.bzender.activities.TenderElectricalDetails"
            android:parentActivityName="apps.pixel.bzender.activities.TenderCarDetails" />
        <activity
            android:name="apps.pixel.bzender.activities.fillDataElectrical.FillDataElectricalActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.fillDataCar.FillDataCarActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.CompleteAddTiner"
            android:parentActivityName="apps.pixel.bzender.activities.before.BeforerActivity" />
        <activity
            android:name="apps.pixel.bzender.activities.webViewContactUs.WebViewActivity"
            android:parentActivityName="apps.pixel.bzender.activities.ContactUs" />
        <activity android:name="apps.pixel.bzender.activities.HowBookTender" />
        <activity android:name="apps.pixel.bzender.activities.HowAddTender" />
        <activity
            android:name="apps.pixel.bzender.activities.ChooseHowItWork"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="5397ab207ad3000ea778a8bb9c620625dd57401b" />

        <activity android:name="apps.pixel.bzender.activities.HowItWork" />
        <activity
            android:name="apps.pixel.bzender.activities.changePassword.ChangePassword"
            android:parentActivityName="apps.pixel.bzender.activities.Setting" />
        <activity
            android:name="apps.pixel.bzender.activities.editProfile.EditProfile"
            android:exported="true"
            android:parentActivityName="apps.pixel.bzender.activities.profile.Profile" />
        <activity
            android:name="apps.pixel.bzender.activities.profile.Profile"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.Setting"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.ContactUs"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.AboutUs"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity android:name="apps.pixel.bzender.activities.WelcomeActivity" />
        <activity
            android:name="apps.pixel.bzender.activities.chatRoom.ChatRoom"
            android:parentActivityName="apps.pixel.bzender.activities.chatRoom.ChatRoom"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="apps.pixel.bzender.activities.notification.Notifications"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.chatAllUsers.ChatListActivity"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.myBooking.MyBooking"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.TenderDetails.TinderDetails"
            android:parentActivityName="apps.pixel.bzender.activities.Tenders.AllTenderActivity" />
        <activity
            android:name="apps.pixel.bzender.activities.Tenders.AllTenderActivity"
            android:parentActivityName="apps.pixel.bzender.activities.before.BeforerActivity" />
        <activity
            android:name="apps.pixel.bzender.activities.AddTender.AddTender"
            android:parentActivityName="apps.pixel.bzender.activities.Home.Home" />
        <activity
            android:name="apps.pixel.bzender.activities.verfication.VerificationActivity"
            android:parentActivityName="apps.pixel.bzender.activities.login.Login"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name="apps.pixel.bzender.activities.forget_password.ForgetPassword"
            android:parentActivityName="apps.pixel.bzender.activities.login.Login" />
        <activity android:name="apps.pixel.bzender.activities.HelloActivity" />
        <activity
            android:name="apps.pixel.bzender.activities.Home.Home"
            android:launchMode="singleTop" />
        <activity
            android:name="apps.pixel.bzender.activities.signUp.SignUp"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name="apps.pixel.bzender.activities.login.Login" />
        <activity android:name="apps.pixel.bzender.activities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/logo_white" />
        <!--
 Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/dark_blue" />

        <service android:name="apps.pixel.bzender.services.MyFireBaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="apps.pixel.bzender.services.SignalRService"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedService" />

        <receiver android:name="apps.pixel.bzender.Utills.NetworkChangeReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>