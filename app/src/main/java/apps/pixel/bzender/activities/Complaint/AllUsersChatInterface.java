package apps.pixel.bzender.activities.Complaint;

import apps.pixel.bzender.Models.chatList.AllUserMessage;

public interface AllUsersChatInterface {
    void getAllUserChatData(AllUserMessage allUserMessage);
}
